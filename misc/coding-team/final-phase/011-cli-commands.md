# Task: New CLI Commands

**Scope:**
- Add to `bin/cli.js`:
  - `palace diff <file>` - Show changes since last scan
  - `palace watch` - Live file watching with auto-refresh
  - `palace stats` - Detailed statistics
  - `palace config` - Manage configuration

**Acceptance:**
- 4 new commands working
- Help text for each
- Unit tests
