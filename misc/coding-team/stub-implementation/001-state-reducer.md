# Task: Implement StateReducer

**Context:** `lib/state-reducer.js` is a stub. Needs Redux-like reducer logic.

**Scope:** `lib/state-reducer.js`, `tests/unit/state-reducer.test.mjs`

**Actions to handle:** `SCAN_START`, `SCAN_COMPLETE`, `FILE_ADD`, `FILE_CHANGE`, `FILE_REMOVE`, `PATTERN_REGISTER`, `FLOW_REGISTER`, `STATE_RESET`

**Acceptance:** Immutable updates, `createReducer(initialState)` factory, unit test 5+ actions.
